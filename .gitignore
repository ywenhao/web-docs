# Dependencies
node_modules

# OS / editor cruft
.DS_Store
*.local
*.log

# Env files (keep .env.example tracked)
.env
.env.*
!.env.example

# Build output
dist
dist-ssr
.output
.nitro
.vinxi
.wrangler

# Cloudflare Workers — generated runtime types (regenerate with `pnpm cf-typegen`)
worker-configuration.d.ts

# Framework / tooling caches
.tanstack
.vite
.vite-plus
.cache
*.tsbuildinfo

# Vite+ git hooks runtime (regenerated by `vp config` on install)
.vite-hooks/_

# Test / coverage
coverage

# Misc generated
__unconfig*
todos.json
