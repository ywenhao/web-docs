# Copy to `.env` and fill in. Only variables prefixed with VITE_ are exposed
# to the client bundle; everything else is server-only.

# Public site URL — used for SEO canonical links, sitemap.xml, and robots.txt.
VITE_SITE_URL=http://localhost:3000
